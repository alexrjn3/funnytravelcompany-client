{
  "name": "client",
  "version": "1.0.0",
  "description": "parte client funtravel",
  "default": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx parcel index.html",
    "build": "parcel build index.html"
  },
  "author": "Rujan Alexandru",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.16.0"
  }
}
